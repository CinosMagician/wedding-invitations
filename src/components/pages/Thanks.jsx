import { motion } from "framer-motion";
import { useEffect, useRef } from "react";
import "./Thanks.css";

export default function Thanks() {
  return (
    <div className="thanks">
      <h1>Thank you for your RSVP - We can't wait to see you there!</h1>
    </div>
  );
}
